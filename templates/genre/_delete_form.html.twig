<form method="post" action="{{ path('app_genre_delete', {'id': genre.id}) }}" onsubmit="return confirm('Voulez vous supprimer ce genre {{ genre.Nom }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete ' ~ genre.id) }}">
    <button class="btn">supprimer</button>
</form>
