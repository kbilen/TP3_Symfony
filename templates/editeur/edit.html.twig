{% extends 'base.html.twig' %}

{% block title %}Modifier l'Éditeur{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-white">
                    <h1 class="h3 mb-0">Modifier l'Éditeur</h1>
                </div>
                <div class="card-body">
                    {{ include('editeur/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
                </div>
                <div class="card-footer bg-light d-flex justify-content-between align-items-center">
                    <a href="{{ path('app_editeur_index') }}" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Retour à la liste
                    </a>
                    {{ include('editeur/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
